# 10-7-2026 Fix Summary

## Commit message

fix: stabilize ticket workflow actions and attachment metadata upload

## Summary

- Disabled demo-blocking backend authorization checks for ticket workflow actions.
- Fixed status actions so Start Progress, Resolve, Start Testing, Close, Reopen, and Reassign persist correctly.
- Refreshed Issue Detail and worklist screens after backend action updates to avoid stale statuses.
- Added REOPEN support for Developer Start Progress visibility.
- Fixed Tester/Developer worklist refresh behavior after navigating back from detail pages.
- Hardened Issue Detail comment/history loading to avoid undefined `issue_id` filters.
- Updated attachment upload to use RAP composition path `/Issue('<id>')/_Attachment` instead of root `/Attachment`.
- Removed unsupported `file_content` upload payload; attachment upload is metadata-only for now.
- Added frontend handling for attachment constraints:
  - generate `file_id` client-side
  - truncate `mime_type` to backend max length 50
  - send `file_size` as string for OData Int64 compatibility
- Updated backend changelog in `BACKEND/docs/03-change-log.md`.

## Notes

Attachment upload currently stores metadata only. Binary file content is not saved until the backend exposes a supported media/content field design.
